# AI 提示词助手 — 使用教学

> [中文](TEACHING.md) | [English](TEACHING_EN.md)
>
> 仓库地址：[https://github.com/Hong-15/AI-Video-Prompt-Assistant](https://github.com/Hong-15/AI-Video-Prompt-Assistant)
>
> 安装、启动参数、锁机制见 [README.md](README.md)

---

## 快速上手

| 步骤 | 操作 |
|------|------|
| 1 | 文件 → 新建项目 → 起名 → 选模板（默认/空） → 选择保存位置 → 创建 |
| 2 | 逐张卡片填提示词，自动保存 |
| 3 | 左侧 "+" 新建任务，独立管理 |
| 4 | 点任务名切换，预览区自动组合内容 |
| 5 | 文件 → 导出，或点预览区"复制" |

---

## 主界面

```
┌──────────────────────────────────────────┐
│  工具栏：文件 | 布局 | 设置 | _ □ X       │
├────────┬─────────────────────────────────┤
│        │                                 │
│ 任务    │          卡片区                  │
│ 列表    │  ┌──────┐ ┌──────┐ ┌──────┐    │
│ [+]    │  │主体   │ │ 场景  │ │光影   │   │
│ 任务1   │  └──────┘ └──────┘ └──────┘    │
│ 任务2   │                                 │
│        │  ┌──────────────┐               │
│        │  │   预览输出    │               │
│        │  └──────────────┘               │
├────────┴─────────────────────────────────┤
│  状态栏：已保存 | 路径 | 任务 | 卡片      │
└──────────────────────────────────────────┘
```

工具栏：
- `[∥]` 切换侧边栏
- 文件：打开文件夹、新建窗口、创建/关闭项目、导入/导出、教学、AI规范、退出
- 布局：重置当前任务布局、重置所有任务布局
- 设置：全局搜索、更多设置

侧边栏：未打开项目时显示"打开文件夹"入口和最近项目列表。

---

## 项目管理

**新建**：文件 → 新建项目 → 输入名称 → 选模板（默认模板：10张预设卡片 / 空模板：1张自定义卡片） → 选保存位置 → 创建。

**打开**：文件 → 打开文件夹，选择含 `userData.json` 的目录；或在侧边栏最近项目中点击。

**关闭**：文件 → 关闭项目。数据自动保存。

**最近项目**：保存最近 20 个，悬停显示 × 可移除单个，路径不存在时灰色标记"已失效"。

---

## 任务管理

点击侧边栏顶部 "+" 新建。名称自动递增：新任务、新任务2…

右键任务 → 重命名 / 删除 / 复制 / 上方插入 / 下方插入 / 重构帧位置 / 导出任务。删除前有确认弹窗，不可恢复。

切换任务自动保存当前数据。

---

## 卡片

### 10 个预设维度

| 卡片 | 填写内容示例 |
|------|------------|
| 主体特征 | "一位穿红色长裙的女性" |
| 场景环境 | "樱花飘落的京都古街" |
| 光影色彩 | "黄金时刻暖色调，柔和侧光" |
| 艺术风格 | "吉卜力动画风格" |
| 镜头景别 | "中景，浅景深" |
| 镜头运动 | "缓慢推近，轻微手持晃动" |
| 时间节奏 | "慢动作，5秒" |
| 动态事件 | "转身微笑，长发随风飘动" |
| 技术参数 | "4K，60fps，HDR" |
| 负面排除 | "不要文字、不要模糊" |

### 操作

- **自定义卡片**：工作区右上角"＋ 自定义卡片" → 输入名字。支持重命名、删除、清空
- **拖拽排序**：按住 "★" 手柄拖动
- **折叠**：点击标题栏折叠/展开
- **调整高度**：拖动卡片底部边缘手柄
- **隐藏**：右键卡片菜单 → 删除，隐藏预设卡片（数据保留）
- **删除自定义卡片**：右键菜单 → 删除（彻底移除）
- **右键导出**：右键卡片 → 导出为 `.md`

---

## 导入导出

**导出**：文件 → 项目数据导出 → 选 Markdown 或纯文本 → 选路径。导出所有任务的所有非空卡片。

**导入**：文件 → 导入项目数据 → 选 `.md`/`.txt` 文件。支持拖拽导入（拖到侧边栏）、粘贴导入（解析数据按钮）。

**拖拽导出卡片**：按住卡片主体区域（非手柄/按钮/输入框）拖出窗口 → 松手导出为 `.md` 文件，保存到桌面。

**拖拽导出任务**：按住侧边栏任务项的序号标记，拖出窗口松手导出。

---

## 设置

**主题**：深色（默认） / 浅色 / 默认 / 跟随系统。

**语言**：中文 / English，切换后重启生效。

**关闭行为**（设置 → 更多设置）：
- 退出应用：× 直接退出
- 隐藏到托盘：× 隐藏到右下角托盘
- 隐藏到任务栏：× 最小化

---

## 快捷键

设置 → 快捷键设置，共 23 个可配置操作。点击键位输入框 → 按下组合键 → 勾选启用。

| 操作 | 默认快捷键 | 默认状态 |
|------|-----------|----------|
| 保存 | Ctrl+S | 启用 |
| 新建任务 | Ctrl+N | 启用 |
| 删除任务 | Delete | 未绑定 |
| 重命名任务 | F2 | 启用 |
| 复制任务 | Ctrl+D | 启用 |
| 清空所有输入 | Ctrl+Shift+A | 未绑定 |
| 复制预览 | Ctrl+Shift+C | 启用 |
| 添加自定义卡片 | — | 未绑定 |
| 切换侧边栏 | Ctrl+B | 启用 |
| 聚焦下一个任务 | Ctrl+↓ | 启用 |
| 聚焦上一个任务 | Ctrl+↑ | 启用 |
| 聚焦下一个输入框 | Ctrl+→ | 启用 |
| 聚焦上一个输入框 | Ctrl+← | 启用 |
| 打开文件夹 | Ctrl+O | 启用 |
| 新建项目 | Ctrl+Shift+N | 启用 |
| 关闭项目 | Ctrl+W | 启用 |
| 导入项目 | — | 未绑定 |
| 导出 Markdown | — | 未绑定 |
| 导出文本 | — | 未绑定 |
| 全局搜索 | Ctrl+Shift+F | 启用 |
| 打开设置 | Ctrl+, | 启用 |
| 重置当前任务布局 | — | 未绑定 |
| 重置所有布局 | — | 未绑定 |

---

## 全局搜索

`Ctrl+Shift+F`，在所有任务的所有卡片内容中全文搜索，点击结果跳转到对应卡片。

---

## AI 规范文档

文件 → AI 规范，查看 AI 数据输出格式规范。顶部中英文切换，底部一键复制。

---

## 数据格式

项目数据存于 `userData.json`：

```json
{
  "tasks": [
    {
      "id": "task_1234567890_abc",
      "name": "提示词任务1",
      "fields": {
        "subject": "一位穿红色长裙的女性",
        "scene": "樱花飘落的京都古街"
      },
      "layoutData": { "subject": 180, "scene": 160 },
      "hiddenFields": [],
      "fieldLabels": {},
      "customCards": [],
      "cardOrder": ["subject", "scene", "lighting", "style"]
    }
  ],
  "activeTaskId": "task_1234567890_abc"
}
```

| 字段 | 说明 |
|------|------|
| `tasks` | 所有任务数组 |
| `tasks[].id` | `task_时间戳_随机串` |
| `tasks[].fields` | 卡片内容，key=卡片标识 |
| `tasks[].layoutData` | 卡片高度（px） |
| `tasks[].hiddenFields` | 已隐藏卡片 key |
| `tasks[].fieldLabels` | 卡片重命名 |
| `tasks[].customCards` | `[{key, label}]` |
| `tasks[].cardOrder` | 卡片显示顺序 |
| `activeTaskId` | 当前激活任务 |

---

## 故障排查

**有进程没窗口**：任务管理器结束所有 Electron / AI提示词助手进程 → 加 `--force` 重新打开。

**"已有实例在运行"**：删掉 `%APPDATA%/ai-prompt-helper/.app-lock.json` → 加 `--force` 打开。

**窗口空白**：检查杀毒软件是否拦截（Smart App Control 可能拦截未签名软件）。日志在 `%APPDATA%/ai-prompt-helper/logs/`。

**关不掉/进程残留**：设置里关闭行为选"退出应用"，或右键托盘退出，或任务管理器强杀。
