const fs = require("fs"); const path = require("path"); let c = fs.readFileSync(path.join(__dirname, "main.js"), "utf-8"); c = c.replace("  win.once('ready-to-show', () => {\r\n    win.show();\r\n  });\r\n\r\n  // 最大化/还原状态同步", "  win.once('ready-to-show', () => {\r\n    win.show();\r\n  });\r\n\r\n  // 修复窗口恢复时的白色闪烁\r\n  win.on('show', () => {\r\n    win.setBackgroundColor('#0b0b1a');\r\n    win.webContents.send('win-show');\r\n  });\r\n\r\n  // 最大化/还原状态同步"); fs.writeFileSync(path.join(__dirname, "main.js"), c, "utf-8"); console.log("main.js updated");